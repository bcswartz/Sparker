'use strict';

angular.module( '<%= grunt.config("sparkConfig").ngapp %>.services' )
    .factory( '<%= grunt.option("name") %>', <%= grunt.option("name") %> );

function <%= grunt.option("name") %> ( <%= grunt.option("dependencyInjections").list %> ) {
    //Object with public methods and properties
    var service = {

    };
    return service;

    //Methods
}

<%= grunt.option("dependencyInjections").injectStatement %>
