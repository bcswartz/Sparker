'use strict';

describe( '<%= grunt.option("name") %>', function () {
    beforeEach( function () {
        <%= grunt.option("controllersModule") %>
        <%= grunt.option("servicesModule") %>
    });

    it( '', function () {

    });

});